using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SFX : MonoBehaviour
{
    /*[SerializeField] private AudioClip Death, EnemyD, HeartObtained, Jump, Shoot;
    Static AudioSource AudS;
    

    // Start is called before the first frame update
    void Start()
    {
        Death = Resources.Load<AudioClip>("Death");
        EnemyD = Resources.Load<AudioClip>("TFK");
        HeartObtained = Resources.Load<AudioClip>("TFK");
        Jump = Resources.Load<AudioClip>("TFK");
        Shoot = Resources.Load<AudioClip>("TFK");

        AudS = GetComponent<AudioSource>();
        // = GetComponent<AudioSource>();
    }

    // Update is called once per frame
    void Update()
    {

    }

    public static void PlaySound(string clip)
    {
        switch (Clip)
        {
            case "Death":
                AudS.PlayOneShot();
                break;


        }
    }*/
}
