using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundEffects : MonoBehaviour
{

    /*[SerializeField] private AudioClip Death, EnemyD, HeartObtained, Jump, Shoot; 

    Static AudioSource AudS;
    // Start is called before the first frame update
    void Start()
    {
        Death = Resources.Load<AudioSource>("Death");
        EnemyD = Resources.Load<AudioSource>("TFK");
        HeartObtained = Resources.Load<AudioSource>("TFK");
        Jump = Resources.Load<AudioSource>("TFK");
        Shoot = Resources.Load<AudioSource>("TFK");

        AudS = GetComponent<AudioSource>();
        // = GetComponent<AudioSource>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public static void PlaySound (string clip)
    {
        switch (Clip)
        {
            case "Death":
                audS.PlayOneShot();
                break;
                

        }
    }*/
}
