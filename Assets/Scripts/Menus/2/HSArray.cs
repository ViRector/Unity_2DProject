using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HSArray : MonoBehaviour
{
    int[] hS = new int[4];
    // Start is called before the first frame update
    void Start()
    {
        hS[0] = 800;
        hS[1] = 600;
        hS[2] = 400;
        hS[3] = 0;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //static void Main(int[])
}
